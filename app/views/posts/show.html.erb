<p id="notice"><%= notice %></p>

<div>
  <%= image_tag @post.image %>
  <% @tags.each do |t| %>
    <span class="tag"><%= link_to t, tag_posts_path(@tag) %> </span>
  <% end %>
</div>

<%= link_to 'Back', posts_path %>
